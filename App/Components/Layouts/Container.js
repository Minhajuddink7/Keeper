import {View} from 'react-native';
import React from 'react';

const Container = ({children}) => {
  return <View style={{marginHorizontal: '4%'}}>{children}</View>;
};

export default Container;
